Place future PEFT or LoRA adapter artifacts here.

Expected logical IDs:

- `ts-migrator`
- `ts-auditor`
- `ts-architect`
- `rust-porter`
- `rust-sentinel`
- `rust-optimizer`

The marketplace runtime already carries these adapter IDs in agent metadata even before real adapters are attached.
