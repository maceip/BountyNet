# Optional: default gateway origin for the dashboard (browser calls this URL).
# Dev server defaults to http://127.0.0.1:8090 when unset.
# Production builds default to https://gateway.stare.network when unset.
# The user can still override in the UI; stored in localStorage (bountynet.gateway).
# VITE_GATEWAY_URL=https://gateway.stare.network
