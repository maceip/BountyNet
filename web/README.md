# BountyNet web (`web/`)

Vite + React + TypeScript UI for **https://bountynet.stare.network**. It talks to the **gateway** (`VITE_GATEWAY_URL`, overridable in localStorage — see `src/App.tsx`).

**Repo docs:** root [`README.md`](../README.md), [`ARCHITECTURE.md`](../ARCHITECTURE.md), [`API.md`](../API.md). **Remaining UX gaps:** [`GAPS.md`](../GAPS.md).

The stock Vite template text that shipped with `create-vite` was removed to avoid conflicting with the above.
